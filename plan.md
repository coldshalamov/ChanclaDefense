1. **Add `sandal_sprite.png` image loading**: I will initialize an `Image` object in `index.html` and `chancla_bomb.html` to load the `sandal_sprite.png` asset.
2. **Update `drawChancla` function**: I will modify the rendering logic to use the loaded `sandal_sprite.png` instead of the text emoji (`🩴`). I will ensure it scales appropriately based on the width (`w`) and height (`h`) properties of the chancla, and differentiate the 'super' chancla (perhaps by adding a tint, glow, or keeping the trailing emoji).
3. **Pre-commit Checks**: I will run the required pre-commit instructions to ensure my changes don't break existing functionality and pass all verifications.
4. **Submit**: I will commit the changes and submit the pull request.
